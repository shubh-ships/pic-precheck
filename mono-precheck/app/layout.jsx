import "./globals.css";

export const metadata = {
  title: "Rotterdam PIC Precheck",
  description: "Prior Informed Consent precheck for chemicals in international trade",
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
